/* Quantumult X parser diagnostic: returns one official-format test node. */
$done({
  content:
    'shadowsocks=example.com:80, method=chacha20, password=pwd, fast-open=false, udp-relay=false, tag=Parser-Diagnostic',
});
